export default rolesPermissions= (name)=>{
    return (req,res,next)=>{
        // const user=await userSchema.findbyId(req.id)
        // if(!user) return next(new ApiError("User Not Found", 404));

        // next()
    }
}